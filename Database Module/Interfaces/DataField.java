package Interfaces;

/**
 * The DataField Interface is implemented by all the types of data the database can have.
 */
public interface DataField {
    /**
     *
     * @return Output put as string so that it can be seen in a text file.
     */
     String getAsString();
}
